# 宝贝启蒙乐园（Toddler Joy Learning）产品需求与技术规格文档 (PRD & Tech Spec)

---

## 1. 项目背景与设计哲学

### 1.1 项目背景
- **目标受众**：3岁半幼儿（学龄前启蒙期），以及陪伴式互动的家长（爸爸）。
- **设备形态**：Web 浏览器可访问、良好适配移动端/平板 H5（优先横屏与大触控体验），并支持打包为 **Android APK** 安装包在平板/手机上全屏运行。
- **产品定位**：一款轻量、有趣、充满亲子温度的多学科儿童启蒙单端应用。涵盖 **英语磨耳朵、古诗诵读、成语故事、传统文化**，通过**阶段微目标**与**即时奖励机制**引导孩子主动开口与探索。

### 1.2 核心设计哲学（专为3岁半儿童定制）
- **零文字负担（Icon & Voice First）**：孩子不识字，全界面依赖大尺寸卡通图标、生动色彩、动效与**全语音播报引导**。
- **微学习单元（Micro-Sessions）**：单个卡片互动控制在 1~2 分钟，一次打卡控制在 5~8 分钟以内，契合低幼注意力时长。
- **听看结合，自信开口**：标准发音领读 + 孩子自我录音即时回放（利用低幼儿童极度爱听自己声音的天性建立开口自信）。
- **单端一体化 + 极简防误触**：无复杂登录、无页面频繁跳转，打开即玩；通过**长按 3 秒**隐藏入口唤出家长管理小抽屉。
- **离线安全与缓存可控**：数据与音频全部存于本地 IndexedDB，断网可用；内置精细化空间清理，防止长期录音撑爆存储。

---

## 2. 整体系统架构与功能全景图

```
┌────────────────────────────────────────────────────────────────────────┐
│                      宝贝启蒙乐园 (单端一体化架构)                       │
├───────────────────────────────────┬────────────────────────────────────┤
│         👶 儿童游乐场 (主界面)       │        👨‍👧 家长小抽屉 (长按3秒解锁)    │
├───────────────────────────────────┼────────────────────────────────────┤
│ • 四大启蒙分类切换 (英语/诗词/成语/文化)│ • 知识库扩容 (免费外部API一键导入)   │
│ • 知识大卡片 (插图/拼音/大字/释义) │ • 手动添加卡片 (支持文字、配图、拼音) │
│ • 标准发音朗诵 (TTS / 爸爸原声音频)│ • 亲子原声录制 (录制爸爸示范音)       │
│ • 3秒极简跟读录音与即时回放       │ • 学习目标配置 (每日目标星数1~10颗)   │
│ • 激励系统 (撒花/星星收集/勋章贴纸)│ • 缓存与存储管理 (专清录音/重置/出厂)  │
│ • 护眼防沉迷 (15分钟小动物睡眠提醒)│ • 护眼时长与发音语速设置             │
└───────────────────────────────────┴────────────────────────────────────┘
```

---

## 3. 详细交互设计与核心流程

### 3.1 儿童游乐主界面

#### 3.1.1 顶部状态栏
- **今日任务进度条**：直观显示今日收集的星星，如 `⭐ ⭐ ⭐ (3/3)`，伴随轻微呼吸动效。
- **勋章/贴纸画册入口**：点击左上角小宝箱 🎁，打开已解锁的卡通贴纸墙。
- **家长抽屉入口**：右上角放置小锁 🔒 或小齿轮 ⚙️，**必须长按持续 3 秒**并显示进度圆环，松手即取消，防止宝宝误触。

#### 3.1.2 四大分类切换栏（大按钮、大间距）
1. **🔤 英语乐园 (English)**：动物、颜色、水果、身体器官、交通工具等名词。
2. **📜 诗词乐园 (Poetry)**：精选音韵悠扬的五言绝句（如《咏鹅》《春晓》《静夜思》）。
3. **🦊 成语乐园 (Idioms)**：经典童话寓言类成语（如《守株待兔》《盲人摸象》《拔苗助长》）。
4. **🏮 文化乐园 (Culture)**：传统节日（春节、中秋、端午）、二十四节气、十二生肖等。

#### 3.1.3 核心卡片交互区（单张全屏大卡片）
- **视觉呈现**：
  - 中央超大区域：卡通高清插画或生动大尺寸 Emoji（如 🍎、🦢、🐇）。
  - 文字展示：超大号汉字/英文单词，字间距大，顶部配注音（带声调拼音或国际音标）。
  - 儿童微释义：1 句简短童趣描述（如：“红红的苹果，甜甜脆脆最好吃！”）。
- **标准示范音（小喇叭 🔊）**：
  - 点击卡片任意主体或点击大喇叭图标，喇叭播放跳动微动画，播报标准发音（优先播放录好的音频/爸爸原声，无音频则调用本地高质量 TTS）。
- **3秒跟读录音（大麦克风 🎤）**：
  - 核心互动入口：右下角常驻醒目的果冻质感大麦克风按钮。
  - **点击触发**：点击后麦克风放大并散发微光，显示“3、2、1”可爱动态倒计时，底部伴随音浪水波纹。
  - **自动完成**：3 秒后自动结束录音，麦克风恢复原状。
- **自我声音回放（核心情绪价值点）**：
  - 录制完毕后，**无需额外操作，系统立刻自动播放宝宝刚录下的稚嫩声音**。
  - 播放时出现小喇叭播放动效，旁边生成一个可重复点击的小按钮（“再听一次我的声音”）。
- **翻页与切换**：
  - 卡片支持触摸左右大滑动手势；
  - 底部两侧常驻大尺寸 **“⬅️ 上一个”** 与 **“下一个 ➡️”** 圆形箭头按钮。

---

### 3.2 游戏化激励与成就体系

#### 3.2.1 即时微反馈（每次录音）
- 宝宝只要完成一次跟读录音：
  1. 屏幕边缘立即喷射彩色纸屑（Confetti 动画效果）。
  2. 伴随欢快的清脆提示音与掌声效：“宝宝念得真好听！太棒啦！”。
  3. 顶部的未点亮星星变成一颗金光闪闪的跳动星星（+1 ⭐）。

#### 3.2.2 每日里程碑目标达成
- 当宝宝集齐今天设定的目标星星（默认 3 颗）：
  1. 暂停当前卡片交互，弹出全屏金奖杯 🏆 庆祝画面。
  2. 动画展示：“恭喜宝贝！完成今天的学习探险啦！”。
  3. **解锁一张新贴纸**：随机掉落一张可爱小动物贴纸（如小恐龙、小金鱼），自动存入“贴纸宝箱”，激发明天的期待。

---

### 3.3 护眼与防沉迷关怀机制

针对 3 岁半宝宝的视力保护与防沉迷设定：
- **单次时长限制**：默认单次使用时长 **15 分钟**（家长可在抽屉中自定义为 10/15/20 分钟）。
- **温柔锁屏机制（小动物睡觉动效）**：
  - 倒计时结束时，屏幕不直接弹冷冰冰的警告框，而是渐变转为宁静的星空背景，伴随舒缓的八音盒摇篮曲。
  - 画面上显示一只闭着眼睛打呼噜的小熊：“小熊打哈欠啦，眼睛要休息咯！我们明天再来玩吧～”。
  - 界面完全锁定，必须由家长在暗角长按 3 秒输入简易验证才能退出或延长时长。

---

### 3.4 家长管理小抽屉（功能详解）

长按 3 秒解锁后弹出的控制面板，包含 4 个管理 Tab：

#### 3.4.1 【添加卡片】专属亲子录入
- **分类选择**：英语 / 诗词 / 成语 / 传统文化。
- **内容输入**：
  - 输入主要词汇/标题（例如：“香蕉 Banana” 或 “咏柳”）。
  - **一键智能补全按钮**：基于轻量汉字拼音转换器，自动生成带音调的拼音和短释义。
- **配图选择**：
  - 预设精选 Emoji 选择盘；
  - 支持调用手机相机拍照或从相册选图（保存为压缩 WebP 图片，节省存储空间）。
- **发音录制（爸爸妈妈的原声）**：
  - 选项 1：系统自动 TTS 合成。
  - 选项 2：**点击现场录制**。爸爸对着手机说一句：“宝贝，这是香蕉，Banana”，保存为音频。
- **即时保存**：点击保存后，卡片写入本地数据库，立刻能在前端刷到。

#### 3.4.2 【题库扩容】一键获取免费资源
- 预设精选扩展包，点击按钮自动下载并导入本地：
  - 📥 `导入10个日常水果与蔬菜英语 (含真人发音)`
  - 📥 `导入10首小学必背五言唐诗`
  - 📥 `导入5个经典寓言成语故事`
  - 📥 `导入中国传统节日与二十四节气`
- 支持接入免费通用大模型接口（如 Gemini API 免费套餐），输入任何词句，自动转换为适合 3 岁宝宝的儿童卡片。

#### 3.4.3 【存储管理】数据与缓存清理
针对平板/手机存储的精细化控制：
- **实时统计看板**：
  ```
  📊 存储占用分析：
  ├── 👶 宝贝录音文件：38 条 (约 14.2 MB)
  ├── 🖼️ 自定义添加卡片：8 张 (约 2.1 MB)
  └── ⚙️ 系统基础配置与缓存：0.3 MB
  总计占用：16.6 MB
  ```
- **三个独立清理按钮（均含二次防呆弹窗）**：
  1. 🧹 **一键清理练习录音**（强烈推荐）：仅删除所有 `.webm/.wav` 录音切片，**保留学习进度、贴纸、自定义卡片**，瞬间腾出 90% 空间。
  2. 🔄 **重置今日学习进度**：清空今日收集的星星，奖杯状态复位（供重新挑战）。
  3. ⚠️ **恢复出厂设置**：清空全部 IndexedDB 本地数据，重新载入系统预置的初始卡片库。

#### 3.4.4 【偏好设置】
- 每日目标星数（1 ~ 10 颗，默认 3 颗）。
- 语音播报语速（0.7x 慢速、0.85x 适合幼儿、1.0x 正常）。
- 单次护眼定时时长（10 分钟 / 15 分钟 / 20 分钟 / 关闭）。

---

## 4. 数据模型与字段规范 (Data Schema)

数据采用本地 IndexedDB（推荐轻量级封装库 Dexie.js）持久化存储。

### 4.1 卡片实体 (`Card`)
```typescript
interface Card {
  id: string;                    // UUID 或短字符串
  category: 'english' | 'poetry' | 'idiom' | 'culture'; // 四大分类
  title: string;                 // 核心标题，如 "Apple"、"咏鹅"、"守株待兔"
  subtitle?: string;             // 副标题或中文翻译，如 "苹果"、"骆宾王"
  phonetic?: string;             // 拼音或音标，如 "[ˈæpl]"、"yǒng é"
  contentLines?: string[];       // 诗词正文分行或多句故事文本
  simpleExplain: string;         // 适合3岁儿童的极短一句话童趣解释
  imageType: 'emoji' | 'local_blob' | 'remote_url'; // 配图类型
  imageData: string;             // Emoji字符、Base64/Blob URL 或静态资源路径
  audioSource: 'tts' | 'local_blob' | 'remote_url'; // 标准音频来源
  audioData?: string;            // 本地录制的爸爸音频Blob、远程MP3直链或空(TTS)
  isPreset: boolean;             // 是否为系统内置初始卡片 (出厂重置时不删除)
  createdAt: number;             // 创建时间戳
}
```

### 4.2 宝宝练习录音实体 (`BabyRecording`)
```typescript
interface BabyRecording {
  id: string;                    // 唯一ID
  cardId: string;                // 关联的卡片ID
  audioBlob: Blob;               // 录音生成的音频二进制文件 (audio/webm 或 audio/wav)
  durationSeconds: number;       // 录音时长（通常 2~4 秒）
  recordedAt: number;            // 录制时间戳
}
```

### 4.3 学习进度与成就实体 (`UserProgress`)
```typescript
interface UserProgress {
  currentDate: string;           // 格式 "YYYY-MM-DD"
  todayStars: number;            // 今日收集的星星数
  targetStars: number;           // 今日目标星数 (默认 3)
  completedCardIds: string[];    // 今日已完成跟读的卡片ID列表
  unlockedStickers: string[];    // 已解锁的贴纸图标代码列表 (如 ["dino", "rabbit"])
  totalStarsAccumulated: number; // 历史总累计星星数
}
```

### 4.4 系统偏好设置 (`AppSettings`)
```typescript
interface AppSettings {
  eyeProtectionMinutes: number;  // 护眼限制时长（默认 15）
  speechRate: number;            // 播报语速 (0.8 ~ 1.0)
  autoPlayAudioOnSwitch: boolean;// 切换卡片后是否自动播放示范音 (默认 true)
}
```

---

## 5. 核心状态机与边界异常处理

```
┌──────────────────────────────────────────────────────────────┐
│                    录音跟读核心状态转换图                      │
└──────────────────────────────────────────────────────────────┘
       [闲置 IDLE]
           │
           │ (点击麦克风)
           ▼
    [检查麦克风权限] ────(权限被拒绝)────▶ [友好弹窗提示家长开启权限]
           │
           │ (授权成功)
           ▼
    [倒计时与录制中 (3秒)] ──(如果正在放TTS则静音并录制)
           │
           │ (倒计时结束)
           ▼
    [保存本地录音 Blob]
           │
           ▼
    [自动回放宝宝声音] ──▶ [触发撒花 Confetti + 星星 + 1]
           │
           ▼
    [返回闲置状态，常驻“再听一次”按钮]
```

### 异常情况保护：
1. **麦克风权限拒绝**：
   - 绝不在宝宝操作时抛出崩溃错误。弹窗显示卡通喇叭并配语音：“请爸爸帮忙打开麦克风小开关哦～”，不影响查看卡片与听标准音。
2. **音频冲突抑制**：
   - 当正在播放标准示范音时，如果宝宝突然按麦克风录音，立即停止播报并开始录音，避免回音污染。
3. **断网离线保障**：
   - 内置离线回退机制。如离线调用外部 API 失败，给出亲和提示：“网络小鸟去休息了，先学学书架里的小知识吧～”。

---

## 6. 首批内置冷启动知识库 (Seed Data 清单)

系统首次启动无需连接任何网络，内置首批 25 个精美卡片直接可用：

### 6.1 英语启蒙 (10个高频高识别词汇)
1. **Apple** 🍎 `[ˈæpl]` - 苹果（红红的大苹果，酸酸甜甜真好吃）
2. **Banana** 🍌 `[bəˈnænə]` - 香蕉（弯弯像个月亮，剥开香香甜甜）
3. **Cat** 🐱 `[kæt]` - 小猫（喵喵喵，小猫抓老鼠）
4. **Dog** 🐶 `[dɔːɡ]` - 小狗（汪汪汪，摇着尾巴好开心）
5. **Duck** 🦆 `[dʌk]` - 鸭子（嘎嘎嘎，在水里游呀游）
6. **Sun** ☀️ `[sʌn]` - 太阳（红彤彤的太阳公公，暖洋洋）
7. **Car** 🚗 `[kɑːr]` - 汽车（滴滴叭叭，小汽车开动啦）
8. **Eye** 👁️ `[aɪ]` - 眼睛（亮闪闪的大眼睛，看美丽的世界）
9. **Nose** 👃 `[noʊz]` - 鼻子（小鼻子闻一闻，真香呀）
10. **Star** ⭐ `[stɑːr]` - 星星（天上一闪一闪的小星星）

### 6.2 诗词乐园 (5首幼儿启蒙五言绝句)
1. **《咏鹅》** 🦢 骆宾王 - 鹅，鹅，鹅，曲项向天歌。白毛浮绿水，红掌拨清波。
2. **《春晓》** 🌸 孟浩然 - 春眠不觉晓，处处闻啼鸟。夜来风雨声，花落知多少。
3. **《静夜思》** 🌙 李白 - 床前明月光，疑是地上霜。举头望明月，低头思故乡。
4. **《悯农》** 🌾 李绅 - 锄禾日当午，汗滴禾下土。谁知盘中餐，粒粒皆辛苦。
5. **《登鹳雀楼》** 🌅 王之涣 - 白日依山尽，黄河入海流。欲穷千里目，更上一层楼。

### 6.3 成语乐园 (5个经典寓言故事)
1. **守株待兔** 🐇 - 每天在树下等小兔子，可不能偷懒哦！
2. **拔苗助长** 🌱 - 禾苗要慢慢长大，太心急拔高它就枯萎啦！
3. **盲人摸象** 🐘 - 摸到耳朵像蒲扇，摸到腿像柱子，看事情要看全面。
4. **狐假虎威** 🦊 - 小狐狸借着老虎的威风吓跑小动物。
5. **井底之蛙** 🐸 - 小青蛙坐在井底，以为天只有井口那么大。

### 6.4 传统文化 (5个核心主题)
1. **中秋节** 🥮 - 八月十五月儿圆，吃甜甜的月饼，看嫦娥姐姐和小玉兔。
2. **过大年 / 春节** 🏮 - 穿新衣、贴春联、放噼里啪啦的红鞭炮。
3. **端午节** 🛶 - 吃糯米粽子，在江上划热热闹闹的龙舟。
4. **十二生肖之鼠/牛/虎** 🐯 - 勇敢聪明的小动物排排队。
5. **春分 / 立春** 🍃 - 天气暖和了，小草发芽，柳树长出绿头发。

---

## 7. 技术栈选型与 APK 打包工程架构

### 7.1 前端核心技术栈
- **核心框架**：`Vue 3` (Composition API, `<script setup>`) 或 `React`。
  - *推荐 Vue 3*：轻量、响应快、心智负担小、低配置 Android 平板运行流畅。
- **构建工具**：`Vite`（秒级启动，打包极小）。
- **样式与适配**：`TailwindCSS` + 自适应视口弹性布局（配合 Safe-area 适配异形屏）。
- **本地存储引擎**：`Dexie.js`（基于 IndexedDB 的类型安全封装，极其方便存储录音 Blob 和查询数据）。
- **特效与动效**：`canvas-confetti`（极轻量的彩带洒满屏幕效果）。
- **拼音自动补全**：`pinyin-pro`（体积小，可在前端直接将汉字转为带音调的拼音）。

### 7.2 声音与录音技术
- **音频采集**：基于浏览器原生 `navigator.mediaDevices.getUserMedia` + `MediaRecorder` API。
  - 自动适配 MIME 类型：优先使用 `audio/webm;codecs=opus`，不支持则降级使用 `audio/wav`。
- **音频回放**：原生 `HTMLAudioElement` 或 `Web Audio API`，支持即时释放内存对象 URL (`URL.revokeObjectURL`)，防止内存泄漏。
- **语音播报 (TTS)**：原生 `window.speechSynthesis`（零网络依赖，免费自带高质量发音）。

### 7.3 Android APK 打包方案（Capacitor 体系）
- **打包框架**：**Capacitor**（由 Ionic 官方维护，现代化替代 Cordova）。
  - **核心优势**：直接将 Vite 打包出的 `dist` 静态资源包装为原生 Android Studio 工程，纯原生渲染，几乎无性能损耗。
- **原生能力配置清单 (`AndroidManifest.xml`)**：
  ```xml
  <!-- 录音权限 -->
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
  <!-- 防止息屏（护眼及读诗时保持常亮） -->
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  ```
- **沉浸模式设置**：
  - 隐藏系统顶部状态栏（StatusBar）与底部导航栏（NavigationBar），进入沉浸式全屏，孩子无法轻易误滑退出。

---

## 8. 开发路线与交付检验标准

### Phase 1: MVP 单端骨架与核心闭环 (预计 1~2 天)
- [x] 完成详尽 PRD 与技术规格规范。
- [ ] 搭建 Vue 3 + Vite + TailwindCSS 纯前端项目工程。
- [ ] 注入首批 25 个内置启蒙卡片库。
- [ ] 完成卡片浏览、TTS 播报、3秒录音、自我回放、撒花与星星收集闭环。
- [ ] 完成长按 3 秒家长抽屉：实现基础卡片手动添加与一键清空录音功能。

### Phase 2: 知识库扩充与细节打磨 (预计 1 天)
- [ ] 接入 Free Dictionary API 获取真实高音质 MP3 发音。
- [ ] 支持家长端录制爸爸示范音并持久化关联卡片。
- [ ] 加入贴纸勋章画册（每满 3 星解锁一张小动物贴纸）。
- [ ] 加入 15 分钟温和护眼休息动画（小动物睡觉）。

### Phase 3: Android APK 封装与实机真机调优
- [ ] 初始化 Capacitor Android 工程并打通构建脚本。
- [ ] 开启全屏沉浸与首次启动麦克风权限友好请求。
- [ ] 在 Android 平板/手机上安装运行测试音频兼容性与触控响应速度。
